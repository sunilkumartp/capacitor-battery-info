import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.batteryinfoapp',
  appName: 'BatteryInfoApp',
  webDir: 'www'
};

export default config;
